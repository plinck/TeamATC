import React from 'react';
import { Card, CardContent } from '@material-ui/core';

const GoogleMap = (props) => {
    return (
        <Card style={{ height: "100%" }}>
            <CardContent>
                <div>
                    <h5>Google map will go here.</h5>
                </div>
            </CardContent>
        </Card>
    )
}

export default GoogleMap